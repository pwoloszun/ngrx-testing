import { Observable } from 'rxjs';

import { RoomDetectorsEffects } from '../room-detectors.effects';

xdescribe('RoomDetectorsEffects', () => {

  it('should be created', () => {
    expect(true).toBeTruthy();
  });
});
