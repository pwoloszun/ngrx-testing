import { AnimateCountToNumberPipe } from './animate-count-to-number.pipe';

describe('AnimateCountToNumberPipe', () => {

  xit('should TODO', () => {
    const pipe = new AnimateCountToNumberPipe();
    expect(false).toEqual(true);
  });

});
